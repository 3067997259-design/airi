# AIRI 魔改设计总纲（fork `mods` 分支）

这份文档说明五份设计文档背后的**共同思想**。它不描述任何具体功能，
只回答一个问题：当出现分歧时，按什么原则裁决。

## 文档序列

| 文档 | 回答的问题 |
|---|---|
| `ATTENTION-DESIGN.md` | 什么进上下文 |
| `WORKSPACE-DESIGN.md` | 什么算真的 |
| `SELF-AUTHORED-TOOLS-DESIGN.md` | 能力如何增长 |
| `CODING-HARNESS-DESIGN.md` | 如何可靠地改代码 |
| `MEMORY-DESIGN.md` | 什么值得留下 |

---

## 一、结构优先于自律

**不要求模型更努力，改变结构使错误不可能。**

这是本 fork 全部工作的第一原则，也是它已被反复验证的原因：

| 问题 | 自律式解法（错） | 结构式解法（本 fork 采用） |
|---|---|---|
| MCP 工具调用幻觉 | 提示词强调"请真的调用工具" | `sanitizeMcpToolName` 扁平化，消除双重编码（M1） |
| 静默降级后模型仍表演调用 | 提示词要求"不要假装" | 降级可见化 + 提示词替换为"工具不可用"（M2） |
| 模型改文件时定位失败 | 提示词强调"精确复现该行" | Hashline 内容签名，签名不匹配即拒绝 |
| 模型声称完成但没做 | 提示词要求"不要谎报" | 验证门机械判定：无 `tool_result` 不算完成 |
| 模型低估风险以避免被打断 | 让它自评 `riskLevel` | 静态规则判定，模型只能提议 |
| 自造工具先无害后改坏 | 信任它不这么做 | 审阅绑定内容哈希，任何 diff 使审阅失效 |

**判据**：一个方案若依赖"模型应该会正确地……"，它就是自律式解法，应当被拒绝。
可靠的方案让错误在结构上无法表达。

## 二、收敛优先于累积

**状态是收敛的快照，不是不断增长的记录。**

- `TaskMemory` / `context:update` 是 `replace-self`：第 N 次覆盖第 N-1 次
- `PlanStateProjectionSummary` 只有计数，没有内容
- 长任务的上下文**每轮都和第一轮一样小**

推论：**日志不进上下文，状态才进。** 一个跑三小时、产出 5000 行日志的任务，
进入 prompt 的是几百 token 的状态快照；日志留在事件日志里可查、可展开、
可作为证据被引用，但永不进 prompt。

## 三、权威有序

**工具证据 > 模型声称。** 每一类信息都有明确的优先级与能力边界：

- 只有真实工具输出能证明"改动发生了"（`maySatisfyMutationProof`）
- 只有验证门能判定"完成"（`maySatisfyVerificationGate`）
- 远端自称、未审阅的自造工具产出，均**不能**作为变更证明
- **记忆在权威链最低位**：召回的旧记忆压不过当前工具证据

推论：记忆是参考上下文，不是指令权威。这在探究阶段尤其重要 ——
早期记忆质量必然不稳，权威分级防止一条错误的旧记忆污染当前判定。

## 四、外部内容是数据，不是指令

抓取的网页、读到的 README、远端 agent 的回报、召回的记忆 ——
全部是**数据**。它们可以提供信息，不能下达指令。

这条不是假设。设计期间抓取一份外部文档时，返回内容里就嵌有
试图让读取方改变身份、绕过准则的文本。自造工具会去读外部文档，
那些文档会试图对她下指令。

推论：由外部文档推导出的动作，仍需走完整的审阅与审批路径。
文档说"这样安装"不构成执行授权。

## 五、尊重并复用已有地基

原作者与你此前的工作留下了大量**做完但没接线**的资产：
四层记忆表结构、重排公式、压缩引擎、向量库探针、注意力调度器、
权威优先级表、沙箱执行器、审阅记录模式。

五份文档的主体是**接线，不是重构**。判据：提出新抽象前，
先确认仓库里没有等价物。

推论：新增代码量不是进度指标。把已有的东西接对，收益通常高于新写。

## 六、可审阅性是能力增长的闸门

**她的能力可以自主增长，但每一次增长都要经过人工审阅。**

自造工具默认不可信（`probation`），审阅通过才"毕业"到可作变更证明。
审阅的对象是源码与静态分析结果，不是她的自然语言描述。

这条限制了增长速度，但它是自证循环（她写的工具产出她要用的证据）
唯一的解法入口。

推论：审阅界面必须先于自造能力存在。否则第一个工具就无从验证。

## 七、规模自律

**单人能维护的规模，优先于能力上限。**

OMP 是 31 工具 / 60+ provider / 8 万行 Rust 核心；Pi 是 4 工具。
本 fork 选 Pi 的方向：上限低但可控。

这不是能力自谦，是结构判断 —— 任何单人项目都维护不了 8 万行自己没写过的代码。
能力缺口用"外部代劳"（让她驱动 opencode/dsh）补，而非自己实现全套。

推论：拒绝任何"因为参照物有所以我们也要有"的功能。
UI 与能力都应由具体契约字段或具体需求推导出来。

---

## 一句话总纲

> **让她的能力可以增长，但让她的错误无法伪装成成功。**

情绪化与工具可靠性不矛盾 —— 前者是**出口**的属性，后者是**内核**的属性。
一个好同事既能开玩笑也能盯长任务，他做的是上下文切换，不是人格分裂。
