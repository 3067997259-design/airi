# LIFE-PLAN：从输入驱动到自主节拍

**日期**：2026-08-29。定位：为本 fork 增加 Neuro-sama 式的**内部节拍**——她不依赖用户输入也能主动开口、选择性沉默、连发、异步。
与 `MAINTENANCE-PLAN.md`（接线断层）、`CAPABILITY-PLAN.md`（能力扩展）并列，三个都指向后续实施。
本文档只记录经过三轮讨论收敛的设计，附分期与落点，**不含实现代码**。

## 一、定位

AIRI 目前是**纯输入驱动**：orchestrator 只在 `send()` 时运行，没有扳机她就是一段静止状态。
用户输入是唯一的触发器，不是"权威信源之一"。

Neuro-sama 之所以让人觉得"活"，核心不是长短期记忆或游戏插件，而是这四种对话节拍能力：

- **主动话头**：直播中不必等待输入也会根据自己的性格侃侃而谈（回复弹幕/SC 但不完全依赖它们）；
- **选择性沉默**：听到别人说话，选择不接话；
- **连发**：你说一句，她说 N 多句；
- **异步**：你下一句话说一半，她还在自己说自己的。

### 舒月世界泡的取舍

舒月 official（AIRP 团队，未公测）的做法是造一个大型"世界泡"程序——给她排每日日程与作息，因此能像朋友一样主动分享。
这个机制适用于"网上好友"人设（人生素材需要虚构）。

但对零这样的**纯数字生命**，虚构日程反而失真：她的"生活素材"就是 AIRI 系统本身——
journal（聊天/工具/计划/审批）、memory 层整合、coding-host 的 workspace、后台进程、一次失败的工具调用……这些每天都在产生，
只是从未被叙述。**她的自述不必编造"早上整理了文件"，她可以说"我刚才把 37 条记忆碎片合并成了 12 条"——因为那是真的。**

### 权威重构声明

接纳自发节拍后，用户输入从"唯一扳机"变为"刺激源之一"。
信任结构不变：权威仍按"工具证据 > 模型声称"（DESIGN-PRINCIPLES §三），
用户输入从来不是信任权威，只是刺激。自发话头只是增加刺激源数量。

## 二、核心设计原则

### 1. 自述与 journal 事实对齐

每一句"我刚才做了 X"都必须对应 journal 里真实发生的 X，可点开审计。
这直接化解用户的安全疑虑（"是不是有关键文件被静默删了"）——她的活动是可查证的、无害的，且比任何编造日程都丰富。

### 2. 考量回合（consideration turn）取代空输入

**空输入的病灶**：聊天补全训练分布里，空 user 消息是分布外输入——模型要么硬挤一段话，要么反问"你想说什么"。用 prompt 解释"空输入代表请你主动"是在对抗训练分布。

**正确方案**：自发轮不伪装成"用户输入"，它是另一种合法的回合一——考量回合。

```
tick 通过便宜门控 → orchestrator 以 source: 'self-initiative' 发起
  → 系统补注携带 ## Self-Initiative 节（"这轮没有用户输入，是否开口由你决定"）
  → 刺激物以结构化上下文注入（真实 journal 事实，非编造用户文本）
  → 本轮只挂两个工具：
      self_speak({ text })  → 成为一条 source='self-initiative' 的真实消息，上屏
      self_note({...})      → 只入 journal 不上屏（"记住了，先不说"）
      什么都不调            → 沉默，tick 落 journal（"考虑过，选择不说"）
```

妙处：

- **工具调用是深度分布内的**——模型对"要不要调这个工具"的判断，远比"面对空输入该说什么"可靠。沉默成为一阶结果，而非失败。
- **Neuro 四种节拍全部落在同一模式**：连发 = 上一轮结束后再跑一次考量回合；沉默 = 没调工具；异步 = 考量回合与用户输入互不阻塞。
- **可审计性白送**："考虑过但选择沉默"也入 journal——她不说的时候，你依然知道她想过。
- 外部内容纪律（§四）自然延续：stimulus 里的事实来自 journal 真实事件，不虚构。

### 3. 不做清单

- 不做自由狂奔的 `while` 循环——每个考量回合都离散、有界、可审计；
- 不给她模式/预算/冷却参数的写权限（结构保证，不是提示词恳求）；
- 暂不上独立"内心小模型"——考量回合直接用当前模型，便宜门控已挡掉多数。运行一段若成本高再优化。

## 三、安全不变量（六条，硬性）

1. **模式写入权不在她手里**——生命模式的配置存主进程/设置侧，她没有任何工具能写它。结构保证。
2. **一切入册**——包括沉默的考量（`life/tick` 事件，`source` 标记）。journal 是她内心生活的黑匣子。
3. **成本闸前置**——便宜规则门控 → 每日自发轮预算（可配，超了 tick 直接静默）→ 冷却计时（复用 `intrusionCooldownMs` 模式）。
4. **连发硬顶**——self-continue 每用户轮最多 N 次，硬上限。
5. **与活跃流互斥**——有活跃流/发送中时不发起考量回合。
6. **静默时段**——日程门控（数字生命的作息：零的"深夜"可以是她的巡检窗口，而非社交时间）。

## 四、生命模式矩阵

### 全局三档（主开关）

| 档位 | 行为 |
|---|---|
| **关闭** | 纯输入驱动 = 今天的现状，随时可回退的安全态 |
| **只回应** | 各触发源照常入 journal，但她不开口 |
| **自主** | 考量回合启用，她会自己开口 |

### 细粒度开关（在自主档下逐项生效）

记忆闪回分享 / 计划里程碑汇报 / 作息习惯 / 连发（每轮上限）/ 异步。

### 与集中模式的合成

集中模式 ≠ 杀死全部自主性，而是**关闭社交信道、保留工作信道**：
闲聊型自发话头静默，但"计划里程碑到了""长任务卡住了"这类工作相关考量照常上报。
实现：扩展 `buildAttentionModeSection` 的 `## Mode` 注入——集中模式下契约明确写"不社交，只报工作"。
这样"作为 agent 工作"和"作为伙伴生活"是同一开关族的两态，而非两套系统。

## 五、分期与落点

> 文件路径为已核实的接线点；实施时以当时仓库为准。

### M1 — mirror 工具（她看见自己）

- 新建 `packages/stage-ui-live2d/src/tools/mirror-tools.ts`
- 数据源全部现成：`custom-parameters` 的 `valuesFor`（当前值）+ `discoveredKeys`（rig 默认值）、`expression-store`（激活表情）、memory store（`valence`/`arousal` 心情）
- 输出自然语言外观快照："发型收着的，披着外套，精灵耳，心情偏平静微暖"
- 进阶形态：接 stage 截图给视觉模型（真·照镜子）
- 注册进 `built-in.ts`（照 `parameter-tools` 体例）

### M2 — 外观 journal 化（她的生活可叙事）

- `core-agent/src/journal/types.ts` 的 `JOURNAL_EVENT_TYPES` 新增 `'appearance/changed'`
- `expression-store` / `parameter-tools` 的变更路径接 `journal.appendActive`
- 外观历史成为可回放的长期记忆："外套是周三开始披的""我上周换过两次发型"

### M3 — 考量回合 + 生命模式（核心批次）

- orchestrator `sendSource` 联合类型 `'text' | 'voice'` 扩展含 `'self-initiative'`（相关 correlation 钩子，行号以当时为准）
- `ChatSendPayload` 加 `source` 字段
- `self_speak` / `self_note` 工具（生命模式 ≠ 关闭时注册）
- 生命模式设置：主进程持久化（同 `memory-host` 持久化模式，`<userData>/life-mode.json`）
- `core-agent/src/journal/types.ts` 新增 `'life/tick'` 事件类型
- tick 服务：主进程（照 `memory-host` 模式）→ Eventa 通知 leader → 领导者 orchestrator 跑考量回合
- 门控顺序：静默时段 → 预算 → 冷却 → 互斥 → 刺激检查（闯入记忆掷骰 / 计划里程碑 / 作息事件 / 未消化 spark）

### M4 — 阶梯路线

- **L0 观测期**：tick 只落 `life/tick`（"这一拍有话可说"）到 journal，不开口。先收集数据看她"想过多少次说话"
- **L1 闯入记忆分享**：机制全在（memory-core intrusion + settings 页参数），只接 spark→自发言论通道
- **L2 作息/在场检测**：日程门控、静音时段、用户在场判断
- **L3 世界泡**：多触发源 + 心情权重 + 计划里程碑分享，与 M3 babysitter 合流（同一 tick 两面：对内推进任务、对外开口分享）

## 六、验收策略

每期按仓库准则：定向 vitest（含 browser 模式测考量回合组件行为）+ agent-browser 真机走查（沿用本项目 `cdp-eval.cjs` 流程）。

**M3 验收清单**：
- 静默时段内零自发轮；
- 预算耗尽后 tick 静默；
- 集中模式（focused）下无社交话头，但工作相关上报仍在；
- journal 可完整回放一次自发决策链（"考虑过→决定说/不说"）。

## 七、与其它计划的关系

- **与 CAPABILITY-PLAN 的 babysitter 合流**：M3 的 tick 与 babysitter 是同一个心跳的两面——对内推进任务、对外开口分享。
- **与 MODS.md 已有纪录关联**：M2.5 分层提示词管线（`## Mode` 注入已存在）、注意力泳道（task/spark 已通）、memory-core 闯入记忆（已实现但从未主动开口）。
